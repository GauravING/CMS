## AI Powered CMS Frontend

### Run locally

1) Install deps

```bash
cd frontend
npm install
```

2) Start dev server

```bash
npm run dev
```

The Vite dev server proxies `/api/*` to `http://127.0.0.1:8000` (Django). Make sure the backend is running.
